//Question-You are given an array of n integers. You want to modify the array so that it is increasing, i.e., every element is at least as large as the previous element.

//On each turn, you may increase the value of any element by one. What is the minimum number of turns required?
/* Input:
5
3 2 5 1 7

Output:
5 */

import java.io.*; 

class Main

{ 
public static void main (String[] args) 

    { 

        int arr[] = {5, 6, 2, 4, 3};

        int arraySum, a;

        arraySum = 0; 

         a = arr[0]; 

        for (int i = 0; i < arr.length(); i ++) 

        { 

           if (arr[i] < a)             

                a= arr[i];            

            arraySum += arr[i]; 

        } 

   int min= arraySum - (arr.length())* a; 

     System.out.println("Minimum Operation = " + minOperation); 

  } 

     } 

} 
